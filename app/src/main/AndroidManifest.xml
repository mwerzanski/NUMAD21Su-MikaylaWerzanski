<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.numad21su_mikaylawerzanski">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mw_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SuMikaylaWerzanski">
        <activity android:name=".LocatorActivity"></activity>
        <activity android:name=".linkCollectorActivity" />
        <activity
            android:name=".clickyActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>